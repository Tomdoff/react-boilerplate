import { DATA_REQUESTED } from 'Constants/action-types';

const initialState = {
    // your state goes here

};

function rootReducer (state = initialState, action) {
    switch (action.type) {
        case action.type === DATA_REQUESTED:
            break;
    }

    return state;
}

export default rootReducer;

